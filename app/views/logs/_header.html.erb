<nav class="navbar navbar-expand-lg bg-info">
  <div class="container text-white">
    <a class="navbar-brand text-white" id="bluelog" href="/">BlueLog</a>
    <% if user_signed_in? %>
      <% if current_page?(root_path) %>
        <p class="my-auto mx-auto header-text header-left-space"><%= current_user.name%>さん こんにちは</p>
      <% end %>
      <% if current_page?(new_log_path) %>
        <p class="my-auto mx-auto header-text header-left-space">ログ作成</p>
      <% end %>
      <% if @log %>
        <% if @log.user_id && current_page?(edit_log_path(@log)) %>
          <p class="my-auto mx-auto header-text header-left-space">ログ編集</p>
        <% end %>
      <% end %>
      <% if current_page?(edit_user_path(current_user)) %>
        <p class="my-auto mx-auto header-text header-left-space">プロフィール編集</p>
      <% end %>
    <% end %>
    <% if @user %>
      <% if @user.id && current_page?(user_path(@user)) %>
        <p class="my-auto mx-auto header-text header-left-space"><%= @user.name%>さんのログ一覧</p>
      <% end %>
    <% end %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <% if user_signed_in? %>
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to 'ログ作成', new_log_path, class: "nav-link text-white" %>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle text-white" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            アカウント
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
              <%= link_to 'マイログ', user_path(current_user), class: "dropdown-item text-dark"%>
              <%= link_to 'プロフィール編集', edit_user_path(current_user), class: "dropdown-item text-dark"%>
              <%= link_to 'ログアウト', destroy_user_session_path, method: :delete, class: "dropdown-item text-dark" %>
          </div>
        </li>
      </ul>
    <%= link_to '魚図鑑', "https://zukan.com/fish/internal4851", class: "fish-book text-white", target: :_blank %>
    <% else %>
      <% if current_page?(new_user_session_path) %>
        <div class="sign-in">
          <p class="my-auto mx-auto header-text">ログイン</p>
        </div>
        <%= link_to '新規登録', new_user_registration_path, class: "nav-link text-white" %>
      <% elsif current_page?(new_user_registration_path) %>
        <div class="sign-in">
          <p class="my-auto mx-auto header-text">新規登録</p>
        </div>
        <%= link_to 'ログイン', new_user_session_path, class: "nav-link text-white" %>
      <% else %>
        <div class="nav-item flex right">
          <%= link_to 'ログイン', new_user_session_path, class: "nav-link text-white" %>
          <%= link_to '新規登録', new_user_registration_path, class: "nav-link text-white" %>
          <%= link_to '魚図鑑', "https://zukan.com/fish/internal4851", class: "fish-book text-white", target: :_blank %>
        </div>
      <% end %>
    <% end %>
  </div>
</nav>